      subroutine fcn(xyz,natoms,force,rmsmax,input_names,
     $                 maxcyc,f,volu,sout,g)
      implicit double precision (a-h,o-z)
      dimension g(3*natoms),xyz(3,natoms)
      character*4 input_names(natoms)
      integer itimes
      logical refcell,usenrm
      save flowest

      factor=.5291772108d0







      
      

      return
      end

